/**
 * Message dependencies.
 */
import "cfsnet/Drive";
import "cfsnet/Operation";

/**
 * The `Request` message represents a request payload for an
 * operation on a CFSNet drive instance.
 */
message Request {
  bytes nonce = 1;
  Drive drive = 2;
  Operation operation = 3;
  bytes buffer = 4;
}
