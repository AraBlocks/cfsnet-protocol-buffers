/**
 * Implements `Read(fd, start, end)` operation.
 * Send this message to read a range from an open
 * file descriptor.
 */
message Read {
  uint32 fileDescriptor = 1;
  uint32 start = 2;
  uint32 end = 3;
}
